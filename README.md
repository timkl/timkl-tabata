Create a tabata workout mp3 from the command-line.

## Installation

Download the repository.

Create a virtual environment:

virtualenv timkl-tabata

Install the dependencies:
pip install

Run the script:
python app.py
